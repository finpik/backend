jar.enabled = true

dependencies {
    api project(":finpik-common")
    implementation project(":finpik-domain:user")
    //TODO(지워질 의존성 빌드가 되게 하기위한 추후 구조 리팩토링 후 제거 예정)
    implementation 'org.springframework:spring-context'
}
